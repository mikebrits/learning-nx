export interface SubmitWord {
  id: string;
  word: string;
}

export interface Word {
  word: string;
  id: string;
  submittedBy: string;
}

export interface MyData {
    date: string;
    time: number;
}
